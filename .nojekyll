# 空文件即可。存在它，GitHub Pages 的「分支发布」路径才不会用 Jekyll 处理产物。
#
# 为什么必须有：Astro 把打包资源放在 dist/_astro/ 下，而 Jekyll 会**跳过所有以 `_` 开头的路径**
# （还有 `.`、`#` 开头与 `~` 结尾的路径）。没有 .nojekyll，线上的 CSS/JS 会全部 404。
# 现状 Hexo 站点恰好没有 `_` 前缀路径，所以一直「侥幸工作」（审计已记录这一点）。
